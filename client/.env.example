# API base URL. Leave empty for same-origin (dev proxy or when served from same host as API).
# Required in production when the client is deployed separately (e.g. Vite/Vercel/Cloudflare Pages).
# VITE_API_URL=https://api.yourdomain.com
